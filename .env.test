NODE_ENV=test

# Integration tests target Docker PostgreSQL exposed on localhost:5433
DATABASE_URL=postgresql://bmad:bmad@127.0.0.1:5433/bmad_stock_agent
POSTGRES_HOST=127.0.0.1
POSTGRES_PORT=5433
POSTGRES_USER=bmad
POSTGRES_PASSWORD=bmad
POSTGRES_DB=bmad_stock_agent

# Test-only fallback secret
JWT_SECRET=test-jwt-secret-minimum-32-characters-long
