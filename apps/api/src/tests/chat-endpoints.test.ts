/**
 * NOTE:
 * Jest est configuré pour exécuter uniquement `src/__tests__/**/*.test.ts`.
 * Le fichier canonique est donc `src/__tests__/chat/chat-endpoints.integration.test.ts`.
 * Celui-ci existe uniquement pour répondre à la contrainte "chemin demandé" (Mission, Étape 5).
 */
export {};

