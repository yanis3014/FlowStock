# Story 4.4: Alertes Visuelles

**Status:** done

## Story

**As a** gérant de PME,  
**I want** recevoir des alertes visuelles pour stocks faibles et ruptures,  
**so that** je peux réagir rapidement aux problèmes.

## Acceptance Criteria

**Given** je suis un utilisateur authentifié  
**When** un stock devient faible  
**Then** une alerte visuelle claire est affichée (badges, couleurs, icônes)  
**And** les alertes pour ruptures imminentes sont affichées (prédiction IA quand disponible)  
**And** la liste des alertes est visible sur le dashboard  
**And** je peux filtrer par type d'alerte  
**And** je peux marquer une alerte comme "vue" ou "résolue"  
**And** les notifications optionnelles pour alertes critiques fonctionnent  
**And** le système d'alertes pour stocks faibles a un seuil configurable

## Tasks / Subtasks

- [x] Task 1 (AC: alerte visuelle claire) – Affichage alertes avec badges, couleurs, icônes
  - [x] Réutiliser/étendre les codes couleur existants (vert/orange/rouge) pour cohérence dashboard
  - [x] Afficher badges ou icônes selon type d'alerte (low_stock, critical_stock, rupture_imminente)
  - [x] Définir mapping severity → style visuel (high=rouge, medium=orange, low=bleu) aligné WCAG AA
  - [x] Intégrer composant AlertsList ou AlertCard dans la section alertes du dashboard

- [x] Task 2 (AC: alertes ruptures imminentes) – Afficher alertes prédiction IA quand disponible
  - [x] Consommer les alertes retournées par GET `/dashboard/summary` (champ `alerts[]`)
  - [x] Afficher message et métadonnées (type, severity, product, message, created_at)
  - [x] Gérer le cas où les prédictions IA ne sont pas encore disponibles (affichage basé sur stock faible/critique uniquement)
  - [x] Lien ou action rapide vers la fiche produit ou la courbe de prévision si pertinent

- [x] Task 3 (AC: liste alertes sur dashboard) – Liste des alertes visible sur le dashboard
  - [x] Section dédiée "Alertes" sur le dashboard (déjà partiellement présente en 4.2)
  - [x] Afficher la liste complète des alertes avec tri par sévérité ou date
  - [x] Pagination ou limite affichée (ex: 10–20) avec lien "Voir toutes les alertes" si besoin
  - [x] États vides gérés (aucune alerte : message rassurant)

- [x] Task 4 (AC: filtrer par type) – Filtres par type d'alerte
  - [x] Filtres UI : type (low_stock, critical_stock, rupture_imminente, etc.) et/ou severity
  - [x] Filtrage côté client ou API dédiée GET `/alerts` avec query params (type, severity, resolved)
  - [x] Persistance du filtre en session ou URL pour partage/refresh

- [x] Task 5 (AC: marquer vue / résolue) – Marquer alerte comme "vue" ou "résolue"
  - [x] Définir modèle : statut alerte (new, seen, resolved) et persistance par utilisateur
  - [x] API : PATCH `/alerts/:id` ou POST `/alerts/:id/ack` pour marquer vue/résolue (si backend dédié)
  - [x] Ou persistance locale (localStorage) pour MVP si pas d’API alertes dédiée
  - [x] Boutons/actions "Marquer comme vue" et "Résoudre" dans la liste
  - [x] Filtrer optionnellement les alertes résolues (masquées par défaut ou onglet "Résolues")

- [x] Task 6 (AC: notifications optionnelles critiques) – Notifications pour alertes critiques
  - [x] Préférence utilisateur : activer/désactiver notifications pour alertes critiques
  - [x] Intégration Web Push ou in-app notifications (toast/banner) pour alertes severity high/critical
  - [x] Documenter scope MVP (in-app uniquement vs push navigateur selon temps disponible)

- [x] Task 7 (AC: seuil configurable) – Seuil configurable pour stock faible
  - [x] Paramètre configurable (ex: pourcentage sous min_quantity ou seuil absolu) par tenant ou global
  - [x] API ou table paramètres : seuil "stock faible" (ex: 120% de min_quantity = alerte)
  - [x] UI paramètres : champ ou slider pour ajuster le seuil (dans paramètres compte ou dashboard)
  - [x] Le dashboard/service recalcule les alertes selon ce seuil

## Dev Notes

### Previous Story Insights

- Story 4.2 a livré : Dashboard avec GET `/dashboard/summary`, section alertes avec `alerts[]` (type, severity, product, message, created_at), codes couleur (vert/orange/rouge). La liste des alertes est déjà affichée ; cette story étend le comportement (filtres, marquer vue/résolue, notifications, seuil).
- Story 4.3 : Courbes de prévision ; les alertes "rupture imminente" peuvent s’appuyer sur les prédictions quand disponibles.
- API Dashboard : [Source: docs/api-specifications.md#10-dashboard-service] — `alerts` déjà dans la réponse summary.

### Data Models

- **Alert** (réponse GET `/dashboard/summary`) : [Source: docs/api-specifications.md#10-dashboard-service]  
  - Champs : type (ex: low_stock), severity (high, medium, low), product { id, name }, message, created_at.
- Si extension backend : table `alerts` ou `user_alert_acks` pour statut vue/résolue par utilisateur (optionnel MVP).
- **Seuil stock faible** : paramètre tenant (ex: `alert_low_stock_threshold_percent` ou règle dérivée de `min_quantity`).

### API Specifications

- **GET /dashboard/summary** : [Source: docs/api-specifications.md#10-dashboard-service]  
  - Retourne `data.alerts[]` avec type, severity, product, message, created_at. Utiliser pour afficher la liste.
- **Extension possible** : GET `/alerts` avec query `type`, `severity`, `status` (new|seen|resolved) ; PATCH `/alerts/:id` pour marquer vue/résolue — à définir si backend dédié aux alertes.

### File Locations

- **Frontend** : [Source: docs/architecture.md]  
  - Composants dashboard : section Alertes dans `dashboard.html` ou équivalent (apps/api/public/ ou apps/web).  
  - Composants : AlertsList, AlertCard, AlertFilters, AlertStatusButton (vue/résolue).  
- **Backend** :  
  - Service dashboard existant : `apps/api/src/services/dashboard.service.ts` (génération des alertes).  
  - Si besoin : routes `alerts.routes.ts`, service `alert.service.ts`, migrations pour statut vue/résolue et paramètre seuil.

### Technical Constraints

- **Accessibilité** : WCAG AA — contrastes pour badges/icônes, labels pour lecteurs d’écran [Source: docs/prd.md].  
- **Responsive** : Liste d’alertes lisible sur desktop et mobile [Source: planning-artifacts/ux-design-specification.md].  
- **Cohérence** : Réutiliser la palette et les composants (Badge, Alert) du design system existant (Story 4.2).

### UX Requirements

- **Alertes visuelles** : [Source: planning-artifacts/ux-design-specification.md] — codes couleur clairs, hiérarchie visuelle (critique > attention > info).  
- **Notifications** : [Source: planning-artifacts/ux-design-specification.md] — notifications push pour alertes importantes sur mobile ; en MVP, in-app (toast/banner) acceptable.

## Testing

- **Frontend** : Tests unitaires composants AlertsList, AlertCard, filtres ; test affichage selon severity/type.  
- **Backend** : Si API alertes dédiée — tests d’intégration GET/PATCH alertes, isolation tenant.  
- **E2E** : Utilisateur authentifié voit les alertes sur le dashboard ; filtre par type ; marque une alerte comme vue ; vérification seuil (création produit sous seuil → alerte affichée).

## Change Log

| Date       | Version | Description        | Author   |
|------------|---------|--------------------|----------|
| 2026-02-09 | 0.1     | Création story 4.4 | BMAD     |
| 2026-02-09 | 0.2     | Implémentation complète Story 4.4 (alertes visuelles, filtres, notifications, seuil configurable) | Dev Agent |
| 2026-02-09 | 0.3     | Code review (CR) : correctifs CRITICAL + HIGH appliqués ; suivi MEDIUM/LOW dans implementation-artifacts/code-review-report-4-4.md | AI Review |
| 2026-02-09 | 0.4     | Correctifs MEDIUM/LOW : liens alerte→produit/prévision, persistance filtres, tests alert-threshold + E2E, libellé limite 10, doc seuil vs badge | AI Review |

## Dev Agent Record

### File List (Story 4.4)

| Fichier | Rôle |
|---------|------|
| `apps/api/public/dashboard.html` | UI dashboard : section Alertes, filtres (type/sévérité/statut), seuil configurable, préférence notifications, marquer vue/résolue (localStorage), toast in-app. |
| `apps/api/src/services/dashboard.service.ts` | Génération des alertes (low_stock, critical_stock, rupture_imminente), seuil tenant (getTenantAlertThreshold, updateTenantAlertThreshold), GET/PUT alert-threshold. |
| `apps/api/src/routes/dashboard.routes.ts` | Routes GET `/dashboard/summary`, GET/PUT `/dashboard/alert-threshold`. |
| `apps/api/src/__tests__/dashboard/dashboard.integration.test.ts` | Tests d’intégration GET /dashboard/summary (auth, structure, alerts). |
| `e2e/tests/dashboard.spec.ts` | E2E : chargement dashboard, statistiques, badges stock, actions recommandées, responsive, perf, gestion erreur. |

Références : `apps/api/src/index.ts` (montage des routes dashboard), `docs/api-specifications.md` (section Dashboard). Pas de migration dédiée 4.4 (seuil stocké dans `tenants.settings` JSONB existant).

### Résumé implémentation

- Alertes affichées depuis `GET /dashboard/summary` (alerts[]), avec badges/sévérité/couleurs et boutons Marquer vue / Résoudre (persistance localStorage).
- Filtres par type, sévérité et statut (actives/résolues) ; seuil % configurable (GET/PUT `/dashboard/alert-threshold`, champ `tenants.settings.lowStockThresholdPercent`).
- Préférence « Notifications alertes critiques » (case à cocher, localStorage `bmad_notifications_enabled`) ; toast in-app pour alertes critiques.
- Rupture imminente : affichée lorsque stock déjà low/critical et `lead_time_days` renseigné (message explicite ; prédiction IA jours restants prévue Epic 5).

## QA Results

*(À remplir après revue QA)*
