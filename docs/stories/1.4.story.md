# Story 1.4: Subscription Tiers Management

**Status:** Done

## Story

**As a** système,  
**I want** gérer les niveaux d'abonnement (Normal, Premium, Premium Plus),  
**so that** les fonctionnalités sont correctement restreintes selon le plan de l'utilisateur.

## Acceptance Criteria

**Given** un utilisateur authentifié  
**When** le système vérifie le niveau d'abonnement  
**Then** le modèle de données pour abonnements existe (niveau, date début, date fin, statut)  
**And** l'API permet de vérifier le niveau d'abonnement d'un utilisateur  
**And** le middleware/guard restreint l'accès aux fonctionnalités selon le niveau (Normal/Premium/Premium Plus)  
**And** l'interface admin permet de gérer les abonnements (souscription, upgrade, downgrade)  
**And** les logs des changements d'abonnement sont enregistrés  
**And** les tests de restriction d'accès par niveau sont passants  
**And** la facturation automatique selon le niveau choisi est configurée (intégration future)

## Tasks / Subtasks

- [x] Task 1 (AC: modèle de données) – Créer migration et modèle Subscriptions
  - [x] Créer migration `V006__create_subscriptions.sql` (ou numéro séquentiel suivant)
  - [x] Créer types ENUM `subscription_tier` ('normal', 'premium', 'premium_plus') et `subscription_status` ('active', 'cancelled', 'past_due', 'trial')
  - [x] Créer table `subscriptions` selon [Source: docs/database-schema.md#2-subscriptions-table] : id, tenant_id, tier, status, started_at, trial_ends_at, current_period_start, current_period_end, cancelled_at, price_monthly, stripe_subscription_id, created_at, updated_at
  - [x] Contrainte une seule subscription active par tenant (index unique partiel sur tenant_id WHERE status = 'active')
  - [x] Index idx_subscriptions_tenant, idx_subscriptions_status
  - [x] Activer RLS sur subscriptions, politique tenant_isolation (tenant_id = app.current_tenant)
  - [x] Exposer type Subscription dans packages/shared/types si nécessaire

- [x] Task 2 (AC: API vérifier niveau) – API Subscriptions
  - [x] Créer route GET `/subscriptions/current` : retourner abonnement courant du tenant (tier, status, started_at, current_period_*, features dérivées)
  - [x] Réponse conforme à [Source: docs/api-specifications.md#11-subscriptions-service] : id, tier, status, started_at, current_period_start/end, price_monthly, features (ai_predictions, smart_orders, photo_invoice, auto_orders, history_days)
  - [x] Protéger route par auth middleware et tenant extraction (comme auth.routes)
  - [x] Créer subscription.service : getCurrentSubscription(tenantId), getFeaturesForTier(tier)
  - [x] Lors de l'inscription (Story 1.3), créer enregistrement subscription trial par défaut si pas déjà fait (aligner avec register/login qui retourne subscription)

- [x] Task 3 (AC: middleware/guard) – Restriction d'accès par tier
  - [x] Créer middleware `subscriptionTierMiddleware` ou `requireTier(['premium','premium_plus'])` : après auth, vérifier subscription du tenant ; si tier insuffisant, retourner 403 FORBIDDEN (feature not available in current subscription tier)
  - [x] Appliquer sur routes qui nécessitent Premium ou Premium Plus (ex: prédictions IA, commandes intelligentes, photo facture) – pour cette story, ajouter sur au moins une route exemple ou documenter les routes à protéger
  - [x] Rate limiting par tier déjà défini dans architecture [Source: docs/architecture.md] : 100/200/500 req/min selon tier – implémenter ou brancher sur config

- [x] Task 4 (AC: interface admin, logs) – Gestion abonnements et audit
  - [x] Créer route POST `/subscriptions/upgrade` (body: new_tier) : mise à jour tier, current_period_*, log du changement ; retourner subscription_id, old_tier, new_tier, effective_date, prorated_amount (calcul optionnel ou placeholder)
  - [x] Créer route POST `/subscriptions/downgrade` (ou même endpoint avec new_tier) : idem, appliquer à la fin de la période courante si besoin
  - [x] Table ou log d'audit des changements d'abonnement (subscription_changes : tenant_id, old_tier, new_tier, changed_at, changed_by) ou log dans une table audit générique
  - [x] Interface admin : page ou section Settings/Subscription pour afficher tier actuel, boutons upgrade/downgrade (liens ou appels API ci-dessus) – scope MVP : au moins GET current + POST upgrade documentés et testés

- [x] Task 5 (AC: tests) – Tests restriction d'accès et API
  - [x] Tests unitaires subscription.service (getCurrentSubscription, getFeaturesForTier)
  - [x] Tests d'intégration : GET /subscriptions/current retourne 200 avec données cohérentes pour tenant authentifié ; retourne 401 sans token
  - [x] Tests d'intégration : POST /subscriptions/upgrade avec new_tier valide met à jour et retourne 200 ; tier invalide 400 ; 403 si non autorisé
  - [x] Test que le middleware de tier retourne 403 pour une route protégée quand le tenant a un tier inférieur (ex: route premium appelée avec tenant normal)

## Dev Notes

### Previous Story Insights

- Story 1.3 a livré : auth JWT, register/login, refresh, logout ; utilisateur associé à un tenant ; réponse login/register inclut déjà `subscription` (trial). S'assurer que la création du tenant crée aussi un enregistrement subscription en statut trial (à ajouter en 1.3 ou dans cette story au plus tôt lors du premier GET /subscriptions/current).
- Migrations existantes : V001 tenants, V002 RLS base, V003 users, V004 refresh_tokens, V005 auth helpers. Prochaine migration = V006 (ou numéro suivant).
- RLS : utiliser les mêmes helpers que pour users (tenant_id = app.current_tenant).

### Data Models

- **subscriptions** (PostgreSQL) : [Source: docs/database-schema.md#2-subscriptions-table]  
  - `subscription_tier` ENUM : normal, premium, premium_plus  
  - `subscription_status` ENUM : active, cancelled, past_due, trial  
  - Une seule subscription active par tenant (contrainte unique partielle).  
- **Features par tier** (dérivées, pas en BDD) : [Source: docs/prd.md, docs/architecture.md]  
  - Normal : pas IA, pas commandes intelligentes, pas photo facture ; history_days 30.  
  - Premium : ai_predictions, smart_orders ; history_days 90 ; pas photo facture ni auto_orders.  
  - Premium Plus : tout + photo_invoice, auto_orders, history illimité.

### API Specifications

- **GET /subscriptions/current** : [Source: docs/api-specifications.md#11-subscriptions-service]  
  - Réponse 200 avec data.id, tier, status, started_at, current_period_start/end, price_monthly, features (ai_predictions, smart_orders, photo_invoice, auto_orders, history_days).  
- **POST /subscriptions/upgrade** : body `{ "new_tier": "premium" | "premium_plus" }` ; 200 avec subscription_id, old_tier, new_tier, effective_date, prorated_amount.  
- **403 FORBIDDEN** : "Feature not available in current subscription tier" [Source: docs/api-specifications.md#error-codes-reference].

### File Locations

- **API** : [Source: docs/architecture.md]  
  - Routes : `apps/api/src/routes/subscription.routes.ts` (ou subscriptions.routes.ts)  
  - Service : `apps/api/src/services/subscription.service.ts`  
  - Middleware : `apps/api/src/middleware/subscriptionTier.middleware.ts` (ou auth + tier dans middleware existant)  
  - Migrations : `apps/api/migrations/V006__create_subscriptions.sql`  
- **Shared** : types Subscription, SubscriptionTier, SubscriptionStatus dans `packages/shared/src/types/` si utilisés par le frontend.

### Testing Requirements

- [Source: docs/architecture.md – Backend] Tests unitaires pour services ; tests d'intégration pour routes (auth déjà en place dans __tests__/auth).  
- Tester isolation tenant : un tenant ne doit pas voir/modifier l'abonnement d'un autre.  
- Tester que le guard/middleware renvoie bien 403 pour un tier insuffisant.

### Technical Constraints

- Pas d'invention de champs : rester aligné sur database-schema.md et api-specifications.md.  
- Facturation automatique (Stripe) : "intégration future" – prévoir stripe_subscription_id et price_monthly dans le schéma, pas d'appel Stripe requis pour cette story.

## Testing

- **Backend** : Jest (déjà configuré apps/api). Tests unitaires subscription.service ; tests d'intégration pour GET /subscriptions/current et POST /subscriptions/upgrade ; test middleware 403 par tier.  
- **Isolation** : RLS et tenant_id vérifiés dans les tests d'intégration (deux tenants, pas de fuite).

## Change Log

| Date       | Version | Description        | Author   |
|------------|---------|--------------------|----------|
| 2026-02-01 | 0.1     | Création story 1.4 | SM (BMAD) |
| 2026-02-01 | 0.2     | Implémentation complète Story 1.4 (subscriptions, API, middleware, tests) | Dev Agent |

## Dev Agent Record

### Agent Model Used

Agent utilisé pour l'implémentation (Dev Story BMAD).

### Debug Log References

Aucun.

### Completion Notes List

- Migration V006 : table `subscriptions` (tier, status, trial_ends_at, etc.) + table `subscription_changes` pour audit des changements de tier. RLS activé sur les deux tables.
- Service `subscription.service.ts` : getCurrentSubscription, getFeaturesForTier, createTrialSubscriptionForTenant (appelé dans register), getSubscriptionForAuth (login), upgradeSubscription (avec écriture dans subscription_changes).
- Routes GET `/subscriptions/current` et POST `/subscriptions/upgrade` ; route exemple GET `/subscriptions/premium-only` protégée par requireTier(['premium','premium_plus']).
- Middleware `requireTier(allowedTiers)` : vérifie le tier du tenant après auth, retourne 403 si insuffisant.
- Auth : register crée une subscription trial si absente ; login retourne la subscription depuis la BDD.
- Tests : unit (getFeaturesForTier, tierSatisfies), intégration (current 401/200, upgrade 401/400/200, premium-only 200/403). Tous les tests passent (85/85).

### File List

- apps/api/migrations/V006__create_subscriptions.sql
- apps/api/src/services/subscription.service.ts
- apps/api/src/services/auth.service.ts (modifié)
- apps/api/src/routes/subscription.routes.ts
- apps/api/src/middleware/subscriptionTier.ts
- apps/api/src/index.ts (modifié : mount /subscriptions)
- apps/api/src/__tests__/services/subscription.service.test.ts
- apps/api/src/__tests__/subscriptions/subscriptions.integration.test.ts
- apps/api/src/__tests__/auth/auth.integration.test.ts (modifié : cleanup subscription_changes, subscriptions)

## QA Results

*(À remplir après revue QA)*
