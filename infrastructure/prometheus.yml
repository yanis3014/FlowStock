# Prometheus configuration for BMAD Stock Agent
# Used by docker-compose when running Prometheus

global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: 'bmad-api'
    static_configs:
      - targets: ['api:3000']
    metrics_path: '/metrics'
